import "./globals.css"
export const metadata = { title: "Affiliate Hunter — Cari yang pasti laku", description: "Hunter affiliate Shopee — scoring terjual x rating, bukan asal viral. FLAT #3368A0." }
export default function RootLayout({children}:{children:React.ReactNode}){
  return <html lang="id"><body>{children}</body></html>
}
